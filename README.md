# Telegram Reels Downloader
Бот принимает ссылку на Instagram Reels и присылает видео-файл пользователю.


## Установка
```bash
python -m venv venv
source venv/bin/activate # Windows: venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
# заполните .env значениями токенов